{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "deno run --unstable-kv --allow-net --allow-read --allow-env --allow-write --allow-run --allow-sys worker.ts",
    "restartPolicyType": "ALWAYS"
  }
}
